{
  "name": "@eduquiz/config",
  "version": "0.0.0",
  "private": true,
  "description": "Configurations partagées (TypeScript, ESLint, Prettier, Tailwind) pour le monorepo EduQuiz.",
  "type": "module",
  "exports": {
    "./tsconfig-base": "./tsconfig-base.json",
    "./eslint": "./eslint.config.js",
    "./prettier": "./prettier.config.js",
    "./tailwind": "./tailwind.config.js"
  },
  "scripts": {
    "lint": "echo \"[@eduquiz/config] lint — rien à linter, paquet de config\" && exit 0",
    "test": "echo \"[@eduquiz/config] test — rien à tester, paquet de config\" && exit 0",
    "typecheck": "echo \"[@eduquiz/config] typecheck — rien à typer\" && exit 0",
    "clean": "rm -rf .turbo node_modules"
  }
}
