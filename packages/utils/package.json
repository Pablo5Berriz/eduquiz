{
  "name": "@eduquiz/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Utilitaires purs partagés (helpers de date, formatage, parsing, validation locale).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rm -rf dist .turbo node_modules"
  },
  "dependencies": {},
  "devDependencies": {
    "@eduquiz/config": "workspace:*"
  }
}
