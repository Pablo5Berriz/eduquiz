{
  "name": "@eduquiz/utils",
  "version": "0.0.0",
  "private": true,
  "description": "Utilitaires purs partagés (helpers de date, formatage, parsing, validation locale).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"[@eduquiz/utils] lint — à configurer étape 0.2\" && exit 0",
    "test": "echo \"[@eduquiz/utils] test — à configurer étape 0.2\" && exit 0",
    "typecheck": "echo \"[@eduquiz/utils] typecheck — à configurer étape 0.2\" && exit 0",
    "clean": "rm -rf dist .turbo node_modules"
  }
}
